name: 📝 Weekly Code Summary to Slack

on:
  schedule:
    - cron: "0 15 * * 5"  # Every Friday at 8 PM PKT (15:00 UTC)
  workflow_dispatch:  # Allows manual runs too

jobs:
  diary:
    runs-on: ubuntu-latest

    env:
      TOKEN_GITHUB: ${{ secrets.TOKEN_GITHUB }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Generate polished weekly summary
        id: diary
        run: |
          node diary.js --polished > summary.txt

      - name: Split summary into chunks
        id: split
        run: |
          awk '/^<<CHUNK>>/ {n++; next} {print > ("chunk" n ".txt")}' summary.txt

      - name: Send polished summary to Slack
        uses: slackapi/slack-github-action@v2.1.1
        with:
          payload-file-path: "./slack-payload.json"
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

      - name: Build Slack message
        run: |
          chunks=""
          for f in chunk*.txt; do
            text=$(cat "$f" | jq -Rs .)
            chunks="$chunks, {\"type\": \"section\", \"text\": {\"type\": \"mrkdwn\", \"text\": $text}}"
          done
          echo "{\"channel\": \"#your-channel-name\", \"blocks\": [${chunks#,}]}" > slack-payload.json
